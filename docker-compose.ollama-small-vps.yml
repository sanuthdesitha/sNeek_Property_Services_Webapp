# Optional starting limits for a shared 2-core / 8-GB VPS. Benchmark before enabling vision.
services:
  ollama:
    cpus: 1.0
    mem_limit: 4g
    memswap_limit: 4g
    environment:
      OLLAMA_CONTEXT_LENGTH: "4096"
      OLLAMA_MAX_LOADED_MODELS: "1"
      OLLAMA_MAX_QUEUE: "2"
