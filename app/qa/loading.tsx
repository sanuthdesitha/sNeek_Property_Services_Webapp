import { BrandLoader } from "@/components/ui/brand-loader";

/** Branded loader for the QA portal (standard token theme). */
export default function QaLoading() {
  return <BrandLoader surface="portal" label="Loading QA portal" />;
}
