import { BrandLoader } from "@/components/ui/brand-loader";

/** Branded loader for the cleaner portal (standard token theme). */
export default function CleanerLoading() {
  return <BrandLoader surface="portal" label="Loading cleaner portal" />;
}
