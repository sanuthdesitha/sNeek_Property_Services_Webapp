import { EstateLoader } from "@/components/v2/ui/estate-loader";

/** v2 segment loading state — the shared Estate mark. */
export default function Loading() {
  return <EstateLoader label="Loading" />;
}
