import { BrandLoader } from "@/components/ui/brand-loader";

/** Branded loader for the laundry portal (standard token theme). */
export default function LaundryLoading() {
  return <BrandLoader surface="portal" label="Loading laundry portal" />;
}
