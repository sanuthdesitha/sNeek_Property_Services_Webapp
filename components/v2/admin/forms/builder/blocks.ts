// ESTATE form builder — structural block constants. Mirrors the value v1 uses
// (components/forms/form-blocks.ts) so divider instruction-blocks authored in
// either builder round-trip identically. Kept local to satisfy the v2 import
// boundary (no imports from @/components/forms/**).
export const DIVIDER_LABEL = "———";
