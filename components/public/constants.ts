/**
 * Shared layout constants for public-site page components.
 * Kept in a standalone file so server and client components can import
 * without pulling in the full PublicSiteShell dependency tree.
 */
export const PUBLIC_PAGE_CONTAINER = "public-page-container mx-auto w-full";
