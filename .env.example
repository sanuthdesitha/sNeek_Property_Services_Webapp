# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/sneek_ops"

# Encryption – at-rest key for property access codes / alarm codes
# REQUIRED. Without it, property create/update refuses to store a code at all
# (it used to silently write the code in plaintext instead — see
# lib/security/encryption.ts). Set this BEFORE deploying, or property saves 500.
# Generate with:  node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Changing this value makes every already-encrypted code undecryptable — rotate
# the stored codes, do not rotate this key casually.
ENCRYPTION_KEY="generate-a-32-byte-hex-key"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# S3 / Cloudflare R2
S3_BUCKET_NAME="sneek-uploads"
S3_REGION="auto"
S3_ENDPOINT="https://<account-id>.r2.cloudflarestorage.com"
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
S3_PUBLIC_BASE_URL="https://pub-<hash>.r2.dev"

# Email – Resend
RESEND_API_KEY="re_xxxxxx"
EMAIL_FROM="noreply@sneekops.com.au"

# AI (optional, server-side only). Enable photo features in Admin > AI configuration.
# Leave blank until a provider credential is configured; never use NEXT_PUBLIC_ keys.
OPENAI_API_KEY=""
OPENAI_VISION_MODEL="gpt-4.1"
OPENAI_TEXT_MODEL="gpt-4.1"
# Social composer provider; photo provider is selected in Admin > AI configuration.
AI_TEXT_PROVIDER="anthropic"
# Local inference: private Ollama endpoint, no cloud-provider fallback.
OLLAMA_BASE_URL=""
OLLAMA_API_KEY=""
OLLAMA_VISION_MODEL="gemma3:4b"
OLLAMA_TEXT_MODEL="gemma3:4b"
# Anthropic remains available for existing settings and the social composer.
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-5"
# Optional dedicated property/section classifier, deployed from services/photo-recognition.
# Keep the service on a private network; both app and service must share this token.
MODEL_SERVICE_URL=""
MODEL_SERVICE_TOKEN=""

# SMS – Twilio (optional)
TWILIO_ACCOUNT_SID="ACxxxx"
TWILIO_AUTH_TOKEN="xxxx"
TWILIO_PHONE_NUMBER="+61400000000"

# App
# Preferred public base URL used in emails and share links (domain or IP)
APP_BASE_URL="https://app.sneekproservices.com.au"
APP_URL="http://localhost:3000"
TZ="Australia/Sydney"
VIDEO_MAX_UPLOAD_MB="300"

# Web Push (VAPID) – browser/PWA push notifications
# Generate a keypair with:  node -e "console.log(require('web-push').generateVAPIDKeys())"
# Keep VAPID_PRIVATE_KEY server-side only. NEXT_PUBLIC_VAPID_PUBLIC_KEY is the public key (safe for the client).
VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_SUBJECT="mailto:admin@sneekproservices.com.au"
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"
